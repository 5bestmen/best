#include "icommplugin.h"
#pragma execution_character_set("utf-8")

/*
//测试
void TextEdit::addError(QString error)
{
    append(error);
    if (this->toPlainText().count() > 100000)
    {
        this->clear();
    }
}

void TextEdit::addLickSuccess()
{
    append("连接成功");
    if (this->toPlainText().count() > 100000)
    {
        this->clear();
    }
}

void TextEdit::addAllBackSuccess()
{
    append("收到总召响应");
    if (this->toPlainText().count() > 100000)
    {
        this->clear();
    }
}

void TextEdit::addOnePoint(int deviceid, int addr, int value)
{
    append(tr("场址:%1   地址:%2      值:%3").arg(deviceid).arg(addr).arg(value));
    if (this->toPlainText().count() > 100000)
    {
        this->clear();
    }
}

void TextEdit::addDisPoint(int deviceid, int addr, int value)
{
    append(tr("场址:%1   地址:%2      值:%3").arg(deviceid).arg(addr).arg(value));
    if (this->toPlainText().count() > 100000)
    {
        this->clear();
    }
}

void TextEdit::addAnalogPoint(int deviceid, int addr, float value, int quality)
{
	append(tr("场址:%1   地址:%2      值:%3    质量:%4").arg(deviceid).arg(addr).arg(value).arg(quality));
	if (this->toPlainText().count() > 100000)
	{
		this->clear();
	}
}

void TextEdit::addAnalogPointFloat(int deviceid, int addr, float value, int quality)
{
	append(tr("场址float:%1   地址float:%2      值float:%3    质量float:%4").arg(deviceid).arg(addr).arg(value).arg(quality));
	if (this->toPlainText().count() > 100000)
	{
		this->clear();
	}
}

*/